name: Test installers

on:
  push:
    paths:
      - scripts/install.sh
      - scripts/install.ps1
      - .github/workflows/test-install.yml
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  install:
    name: ${{ matrix.name }}
    runs-on: ${{ matrix.os }}
    timeout-minutes: 30
    strategy:
      fail-fast: false
      matrix:
        include:
          - name: linux-x86_64
            os: ubuntu-latest
            kind: unix
          - name: linux-aarch64
            os: ubuntu-24.04-arm
            kind: unix
          - name: macos-aarch64
            os: macos-latest
            kind: unix
          - name: macos-x86_64
            os: macos-13
            kind: unix
          - name: windows-powershell
            os: windows-latest
            kind: pwsh
          - name: windows-cmd
            os: windows-latest
            kind: cmd

    steps:
      - uses: actions/checkout@v4

      - name: Install (Unix)
        if: matrix.kind == 'unix'
        shell: bash
        run: |
          set -euo pipefail
          bash scripts/install.sh
          export PATH="$HOME/.local/bin:$PATH"
          command -v supercharge
          supercharge --version
          sc --version

      - name: Install (Windows PowerShell)
        if: matrix.kind == 'pwsh'
        shell: powershell
        run: |
          $ErrorActionPreference = 'Stop'
          & .\scripts\install.ps1
          $cmd = Get-Command supercharge -ErrorAction Stop
          Write-Host "Resolved: $($cmd.Source)"
          & supercharge --version
          & sc --version

      - name: Install (Windows cmd)
        if: matrix.kind == 'cmd'
        shell: cmd
        run: |
          powershell -NoProfile -ExecutionPolicy Bypass -File scripts\install.ps1
          if errorlevel 1 exit /b 1
          "%USERPROFILE%\.local\bin\supercharge.exe" --version
          if errorlevel 1 exit /b 1
          "%USERPROFILE%\.local\bin\sc.exe" --version
          if errorlevel 1 exit /b 1
          where supercharge
